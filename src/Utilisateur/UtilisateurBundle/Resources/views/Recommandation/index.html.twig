{% extends('::layout/layout.html.twig') %}

{% block body -%}
    <h1>Recommandation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dateajoutrecommandation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('recommandation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateAjoutRecommandation %}{{ entity.dateAjoutRecommandation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('recommandation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('recommandation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('recommandation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
