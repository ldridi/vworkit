{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Plaisir list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomplaisir</th>
                <th>Descriptionplaisir</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('plaisir_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nomPlaisir }}</td>
                <td>{{ entity.descriptionPlaisir }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('plaisir_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('plaisir_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('plaisir_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
