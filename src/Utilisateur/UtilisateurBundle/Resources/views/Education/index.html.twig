{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Education list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titreeducation</th>
                <th>Instituteducation</th>
                <th>Datefineducation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('education_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titreEducation }}</td>
                <td>{{ entity.institutEducation }}</td>
                <td>{{ entity.dateFinEducation }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('education_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('education_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('education_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
