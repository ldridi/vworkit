{% extends '::layout/layout.html.twig' %}

{% block body -%}
    <h1>Portfolio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titreportfolio</th>
                <th>Imageportfolio</th>
                <th>Descriptionportfolio</th>
                <th>Dateajoutportfolio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('portfolio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titrePortfolio }}</td>
                <td>{{ entity.imagePortfolio }}</td>
                <td>{{ entity.descriptionPortfolio }}</td>
                <td>{% if entity.dateAjoutPortfolio %}{{ entity.dateAjoutPortfolio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('portfolio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('portfolio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('portfolio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
