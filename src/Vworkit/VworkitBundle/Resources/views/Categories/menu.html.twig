<a href="#" class="list-group-item"><b>Recherche par categorie</b></a>
{% for categorie in categories %}
    
    <li class="list-group-item"><img src="{{ asset(categorie.image.webPath) }}" width="50"><span class="badge badge-default">{{nb}}</span>&nbsp;<a style="color:black" href="{{path('categorieUser',{'categorie' : categorie.nomCategories} ) }}">{{ categorie.nomCategories }}</a></li>

{% endfor %}
