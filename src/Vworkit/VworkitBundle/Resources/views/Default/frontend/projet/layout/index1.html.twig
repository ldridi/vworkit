{% extends '::layout/layout.html.twig' %}

{% block body -%}
    <h1>Projet list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titreprojet</th>
                <th>Descriptionprojet</th>
                <th>Etatprojet</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('projet_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titreProjet }}</td>
                <td>{{ entity.descriptionProjet }}</td>
                <td>{{ entity.etatProjet }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('projet_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('projet_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('projet_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
